using System.Collections;
using System.Collections.Generic;
using UnityEngine;

class Button_Log : Base_Log
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
